% [dxdt_test, u_test, F_test] = robot_dyn_stance_jump(t,X,p)
close all
plot(test.F(2,:))
hold on
plot(test.F(1,:))
% plot(test.tau(2,:))
% plot(test.tau(3,:))
% legend('Tau1_{FF}','Tau2_{FF}','Tau1_{PD}', 'Tau2_{PD}')

%%
% close all
% t = 50
% p = get_params();
% % q = X(t,1:3);
% q = [ 0.1 , -pi/4  ,pi/2]';
% J14 = fcn_J14(q,p.params);
% p14 = fcn_p14(q,p.params)
% 
% % F = test.F(:,t)
% F = [0 ; 10];
% tau = J14'*F
% plotRobot(q,p)

%% 
close all
figure()
% plot(t_out,F_out(:,2), 'LineWidth',2)
hold on
% yline(p.threshold,'--k','LineWidth',2)
% plot(t_out,U_out, 'LineWidth',1)
plot(t_out,X_out(:,2:3))
% plot(t_out,X_out(:,5:6))

%%
i = 194;
test.x = zeros(0);
test.F = zeros(0);
test.tau = zeros(0);
robot_dyn_stance_jump(t_out(i),X(i,:),p,test)
plotRobot(X_out(i,1:3),p)

%% 
if true
    fprintf("poo")
end